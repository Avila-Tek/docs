---
sidebar_position: 2
title: Reglas del código
---

import Note from '@site/src/components/Note/index.mdx';

## 1. Convenciones de nombres

###  Carpetas

- Los nombres de carpetas deben estar en formato camelCase.
- Los nombres de carpetas deben estar en formato snake_case **(only for mobile)**.

```typescript
// ❌ Mal
lib/
  ├── SentryConfig/
  └── .../

// ✅ Bien
lib/
  ├── sentryConfig/
  └── .../
```

###  Archivos

- Los nombres de archivos deben estar en formato PascalCase.
- Los nombres de archivos deben estar en formato snake_case **(only for mobile)**.

```typescript
// ❌ Mal
lib/
  └── SentryConfig/
        └── SentryConfig.ts

// ✅ Bien
lib/
  └── sentryConfig/
        └── sentryConfig.ts
```

###  Variables

Los nombres de variables deben estar bien definidos y ser descriptivos.

```typescript
// ❌ Mal
const a = 25;
const b = "John";

// ✅ Bien
const age = 25;
const firstName = "John";
```

###  Funciones

Los nombres de funciones deben estar bien definidos y ser descriptivos.

```typescript
// ❌ Mal
function a() {
    return 25;
} 

// ✅ Bien
function getAge() {
    return 25;
}
```

### Rutas absolutas (obligatorio)

**Regla**

- ✅ Importa usando alias `@/` (rutas absolutas).
- ❌ Evita `../../..` fuera de la carpeta local.

**Por qué**

- Mejora refactors, reduce errores, acelera lectura en PR.

**Ejemplos**

```ts
// ✅ bien
import { Button } from '@/shared/ui/button';
import { ProductCard } from '@/entities/product';
import { AddToCartButton } from '@/features/cart/add-to-cart';

// ❌ mal
import { Button } from '../../../shared/ui/button';
```

<Note title="Nota" type="warning">
Esto puede variar si es para `web` o para `mobile`.
</Note>